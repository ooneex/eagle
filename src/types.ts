/**
 * Type representing primitive scalar values in TypeScript:
 * - boolean: true/false values
 * - number: numeric values like integers and floats
 * - bigint: arbitrary precision integers
 * - string: text values
 */
export type ScalarType = boolean | number | bigint | string;
